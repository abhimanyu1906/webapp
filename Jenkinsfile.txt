pipeline {
	agent any
	
	stages{
		stage('Build'){
            		steps {
                		sh 'mvn clean package'
            		}
            		post {
                		success {
                    			archiveArtifacts artifacts: '**/target/*.war'
                		}
            		}
		}
		stage('deploy') {
			steps {
				sh 'scp -i /opt/a1/new.pem ./target/*.war ec2-user@172.31.43.90:/opt/apache-tomcat-7.0.53/webapps'
			}
		}
	}
}
